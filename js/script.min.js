const hide=t=>{t.style.display="none"},show=(t,o)=>{t.style.display=o},mapButton=document.querySelector(".about-contacts-img"),mapModal=document.querySelector(".modal-map");if(mapModal){const t=mapModal.querySelector(".modal-close");mapButton.addEventListener("click",t=>{t.preventDefault(),show(mapModal,"block")}),t.addEventListener("click",t=>{hide(mapModal)})}const contactButton=document.querySelector(".button-contacts"),contactModal=document.querySelector(".modal-login");if(contactModal&&contactButton){const t=contactModal.querySelector(".modal-close");contactButton.addEventListener("click",t=>{t.preventDefault(),show(contactModal,"block")}),t.addEventListener("click",t=>{hide(contactModal)})}const catalogButtons=document.querySelectorAll(".buy"),catalogModal=document.querySelector(".modal-catalog");if(catalogButtons.length&&catalogModal){const t=catalogModal.querySelector(".modal-close");Array.from(catalogButtons).forEach(t=>{t.addEventListener("click",t=>{t.preventDefault(),show(catalogModal,"flex")})}),t.addEventListener("click",t=>{hide(catalogModal)})}